ZzzAutoSign {{TAG}}

绝区零（Zenless Zone Zero）每日自动签到工具，Windows 托盘常驻。

自包含单文件发布，**无需另外安装 .NET 运行时**，解压即用。仅支持 Windows x64。

本版本由 CI 在 `windows-latest` 上构建，产物见下方 Assets，校验和见 `SHA256SUMS.txt`。

> 想给某个版本写更详细的说明，在仓库里添加 `docs/release-notes/{{TAG}}.md` 后再打 tag，
> CI 会自动把它作为 Release 正文。使用说明与排障见仓库 README 与 `docs/`。
